import S from './styles.module.css';

interface I{{name}}Props {
  children?: React.ReactNode;
}

export const {{name}} = ({ children }: I{{name}}Props) => {
  return (
    <div className={S.root}>
      {children}
    </div>
  );
};